const fs=require('fs'),p='src/components/EbayAutomaticOperations.tsx';let s=fs.readFileSync(p,'utf8');const old=fs.readFileSync('.codex-tmp/image-display-deploy/src/components/EbayAutomaticOperations.tsx','utf8');const a=old.indexOf('  return (',old.indexOf('  const active ='));const b=old.indexOf('      {Object.entries(imageJobs).map',a);const pos=s.indexOf('      {(["EBAY", "SHOPIFY"] as const).map(kind =>');s=s.slice(0,pos)+old.slice(a,b)+s.slice(pos)+'    </div>\n  );\n}\n';fs.writeFileSync(p,s);
