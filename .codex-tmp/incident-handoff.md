# Active Shopify image incident — 2026-09-10

User reports supplier/Pocamarket image on Shopify SKU 101214. Do not stop at explanation. Continue recovery until all possible repairs verified. Do not publish unchecked galleries. User authorized fixing existing listings; images already approved in app are used; no price/inventory/option composition changes.

Production latest: dpl_GDU7zJMdm69YZBhaabKzGkBxMjEq, https://ebay-order-manager-cdtp1n9m1-tngks2313-9711s-projects.vercel.app, promoted to https://ebay-order-manager-lake.vercel.app. Compact stage .codex-tmp/image-display-deploy only (never repo root deployment). Latest tests: 426 / 79 files, lint 0 errors 19 existing warnings, local+Vercel build success. No schema changes, no commits, huge dirty repo; don't reset/blanket commit.

Containment: all 118 Shopify products audited. Unpublished 15 newly exposed + 39 old public raw + 29 old public raw with Shopify filename suffix = 83 affected/recovery listings. One valid own-photo active listing + archived products left unchanged. Backups: shopify-incident-before.json, shopify-incident-extra-before.json, shopify-incident-suffix-before.json (each .nodes with before media IDs). Original full product/variant IDs+SKU snapshot: incident-all-media.json. Don't rerun containment scripts: some products are now correctly restored!

Initial 11 singles image job cmtv06rdc0001kx04k5pbiucz completed 11/11. All 11 then restored after new server gallery guards. SKU101214 local ad96512c-c457-4766-a3fd-f7300a757d47, Shopify15252533510512, public handle stray-kids-skz-i-n-official-stay-in-stay-in-jeju-exhibition-photocard-kpop-3. Visually verified actual public page has exactly 1 approved processed image with own background/watermark; no original. CUA incidentTab tab6. Same eBay158244951303 checked tab7: exactly1 worked image, no extra raw. Don't alter eBay unrelated settings.

Active 43-item IMAGES job: cmtv0lyt00001jr041115v0wq. Input incident-remaining-job-input.json, result incident-remaining-job.json, live status incident-remaining-job-status.json. Includes39 extra + NOEASY and3 BTS groups. .codex-tmp/poll-shopify-incident-job.cjs live exec session18377 polls authenticated GET every6s (triggers next bounded worker). NOTE this first poll script terminal list accidentally lacks COMPLETED_WITH_ERROR; if errors occur, stop helper after collecting results. It can expire after300 loops; server cron still continues job. Each group can take minutes (48-option DICON completed after~165s, not failure). Do not rush overlapping leases.

Next 29 suffix-image listings: input incident-suffix-job-input.json, matches incident-suffix-matches.json. Local continuation .codex-tmp/continue-shopify-incident.cjs live session97887 watches43 status file, then creates29 IMAGES job once first completes and starts .codex-tmp/poll-shopify-suffix-job.cjs. Actual next job ID will be in incident-suffix-job.json, live status incident-suffix-job-status.json. Do NOT create duplicate job while this helper is waiting/running. Initial setup command had a quoting error; corrected files were subsequently written and node --check passed; waiter is running normally. Check process/status files if in doubt.

Restoration watcher: .codex-tmp/restore-verified-shopify-galleries.cjs session74895. Reads all83 before media IDs+current GraphQL snapshots. Only when every original media ID is gone and all new media READY, calls authenticated app POST /api/products/<localId>/shopify-link with confirmed:true. App independently prepares/validates approved gallery before publish. Stores incident-restoration-snapshot.json and incident-restoration-results.json. Results list excludes initial11 already restored. Total restored roughly11+successful watcher results. Skips a blocked unchanged media fingerprint until manually retried. Watcher can expire after150 loops (~50m plus request time); restart if needed, already-published rows skip safely. Must inspect non303 results and fix, not blindly republish.

Read status summaries only (don't dump secrets): node loads job.status, processedCount,successCount,failureCount, items.filter(error); watcher result.status===303 counts + non303 entries. Last known first job26/43 success0fail and watcher26 successes, total37/83 restored. Current may have advanced.

Full independent verification: run node .codex-tmp/audit-shopify-all-media.cjs > .codex-tmp/incident-verification-progress.json then node .codex-tmp/verify-shopify-incident.cjs. Checks restored listings' exact variant ID+SKU preservation, old media IDs absent, group variant image URLs belong to new gallery, no public raw32hex file patterns. report incident-verification-report.json. Last verified31 restored, issues[], publicRaw[]. Repeat after final recovery and for current progress. All max media49/max variants48 so first100 audit not truncated; products118 hasNextPagefalse.

Credentials: @next/env local Shopify client credentials work (fresh OAuth client_credentials scoped read/write_publications). Never print secrets. Authenticated app curl header file .codex-tmp/bts-request-headers.txt (do NOT print contents). Production DB env copied files are sanitized, not usable. Shopify direct audit script fetches only product data; no orders/PII. Don't rerun mutation scripts blindly.

Implemented source changes:
- src/lib/listing-source-images.ts: remove sourceImageUrl and legacy ebayImageUrls fallback; approved metadata/userFront or explicit positive history required; reject URL equal sourceImageUrl; reject pocamarket.com/infludeo.com and /media/photocard_blur/ even if mislabeled Lens; DB read includes imageSource/sourceImageUrl/userFrontImageUrl; prepareProductListingSource exported for approved thumbnail sources.
- src/lib/shopify-gallery.ts + tests: full READY gallery exact expected filename/hash identities, count/no duplicates, first cover, no truncated pages; polls ingestion up12s; rejects any extra/missing/unapproved image.
- shopifyService: publishExistingShopifyProduct now requiresuserId, prepares all linked approved images + matching latest READY thumbnail and checks full gallery before publish. New single/group publication also guard. group thumbnail uses approved sources, not rawproduct image. syncShopifyImages requiresadminuser; when includeWholeGroup=true rebuilds exact stored Shopify group's approved cover+option images; confirms actual option IDs+SKUs, newmediaREADY, links variants (checksreturnedmedia IDs), then deletesoldgallery+checksnewfullgallery. Shared gallery replacement extractedhelper. No quantity/price changes in IMAGES. Single full upload also refuses actual multi-variant target before modifying.
- channel-publish-jobs calls syncShopifyImages(product,userId,true) for IMAGES.
- shopify-link routePOST passesuser.id,maxDuration300; guidance now explainsimageverification rather than only permissions.
- Tests425then426 newest allpass; final deployment includes all changes. No further code changes currently pending except docs notes.

Docs engineering-notes new incident section. findings.md new top incident section still says OPERATING RECOVERY IN PROGRESS. Update it with final counts/results after jobs done, don't leave false complete claim. Previous earlier publication repair section still historically15; new incident explicitly corrects HTTP200/title insufficient image verification.

User updates Korean <=60s during active work. Current user aware83 recoverytargets,101214fixed,large48groupcompleted. Don't give final until recovery dealt with. If approved sources missing, keep affected private and report concrete blockers; never substitute Pocamarket/candidates. Do not spawn subagents (notauthorized). No need more speculative feature scope. Final should own mistake, explain old mixed gallery + unvalidated publication, actual recovered counts and safe remaining limitations, link101214 ifuseful. Need ensure local helper sessions finished/stopped before ending. Active server job maycontinue via cron but don't abandon authorizedwork.

UPDATE: First43 job COMPLETED 43success0fail. Suffix29 job cmtv1vplj0002jq040m18d144 created by waiter; live status incident-suffix-job-status.json, same parent execsession97887 now running child poll. Initial11+43 =54 galleries repaired. Restoration watcher session74895 continues all83.

FINAL: All83 galleries repaired and republished. Independent verification restored83, issues[], publicRaw[]. All118 audited; active84/public84/allREADY; no supplier/import-record description matches. Last29 job COMPLETED29success0fail. Helpers97887 and74895 exited0. Additional3 BTS descriptions cleaned of CSV import JSON; shared description builder now rejects internal import memo (including truncated JSON), legacy plain copy preserved. Latest tests429/79files, lint0errors19existingwarnings, local+Vercel buildpassed; final production dpl_6qkuHeaWMA3jceuycY12uGTsHnLH / ebay-order-manager-8jpyva352-tngks2313-9711s-projects.vercel.app promoted; health200. Docs findings marked recovered. Separate price observation: DICON Shopify15239144669552 sixvariants82804/82805/82806/82808/82811/82814 priced USD1000-3000, not changed by IMAGES; recorded in findings, cause unresolved. User informed in commentary.
