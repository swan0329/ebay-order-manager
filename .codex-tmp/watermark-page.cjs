const fs=require('fs');const d='outputs/card-layout-review-2026-09-11';let h=fs.readFileSync(d+'/index.html','utf8');h=h.replace('카드 크기 비교 · 판매 반영 전','워터마크 수정 전후 · 판매 반영 전').replace('카드 크기 비교 · 판매 반영 전','워터마크 수정 전후 · 판매 반영 전').replace('왼쪽은 현재 제작 규격, 오른쪽은 세로 2:3 캔버스와 줄인 여백입니다. 같은 화면 상자에 넣어 비교합니다.','왼쪽은 직전 비교안, 오른쪽은 가장자리 워터마크 배치를 수정한 결과입니다. 양쪽 모두 같은 이미지 크기입니다. 저장된 로고·투명도·크기·간격은 바꾸지 않았습니다. 묶음 워터마크는 이미 가장자리 잘라 표시를 사용하므로 이번 수정 전후가 같습니다.').replaceAll('-before.jpg','-watermark-before.jpg').replaceAll('현재 960×1200','수정 전 800×1200').replaceAll('현재 1000×1000','수정 전 1000×1000').replaceAll('.jpg"','.jpg?v=wm2"');fs.writeFileSync(d+'/watermark.html',h);let original=fs.readFileSync(d+'/index.html','utf8').replace('<header>','<header><p><a href="watermark.html">워터마크만 수정 전후 비교 보기</a></p>').replaceAll('.jpg"','.jpg?v=wm2"');fs.writeFileSync(d+'/index.html',original);fs.copyFileSync('src/lib/narrow-review.test.ts',d+'/engine/review-tests.ts.txt');fs.appendFileSync(d+'/REVIEW.md','\n워터마크 재검토: 현재 운영 설정과 비교안 설정 일치 확인. 개별 렌더러의 음수 좌표를 0으로 강제하여 로고를 안쪽으로 옮기던 오류를 후보에서 수정했다. 원본 타일의 화면 안 영역만 추출하여 격자 중심을 보존한다. 세 가지 테스트 통과(실원본/모서리·회전·그림자·배경/폭보다 큰 로고). 개별 실제 결과 시각 확인. 그룹은 기존 잘라 표시 경로를 유지했다. 생산 코드 배포 및 외부 이미지 교체 없음.\n');
