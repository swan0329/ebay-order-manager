const fs=require('fs');const {call}=require('./procurement-live.cjs');(async()=>{for(const [key,path] of [['channel','/api/channel-publish-jobs'],['ebay','/api/ebay/operations?history=true']]){const r=await call(path);fs.writeFileSync('.codex-tmp/procurement-active-'+key+'.json',JSON.stringify(r));console.log(key,JSON.stringify(r.data.jobs?.map(j=>({id:j.id,status:j.status,mode:j.mode,total:j.totalCount,success:j.successCount,createdAt:j.createdAt,submittedAt:j.submittedAt}))));}})();
