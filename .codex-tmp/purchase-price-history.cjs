const fs=require('fs');const cookie=fs.readFileSync('.codex-tmp/bts-request-headers.txt','utf8').split(/\r?\n/).find(x=>/^cookie:/i.test(x)).replace(/^cookie:\s*/i,'');
(async()=>{for(const [key,path]of Object.entries({operations:'/api/ebay/operations?history=true',syncsettings:'/api/pocamarket-sync/settings',progress:'/api/pocamarket-sync/progress',catalog:'/api/pocamarket-catalog'})){const r=await fetch('https://ebay-order-manager-lake.vercel.app'+path,{headers:{cookie}});fs.writeFileSync('.codex-tmp/purchase-price-'+key+'.json',await r.text());console.log(key,r.status)}})();
