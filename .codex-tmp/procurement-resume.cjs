const fs=require('fs');const cookie=fs.readFileSync('.codex-tmp/bts-request-headers.txt','utf8').split(/\r?\n/).find(x=>/^cookie:/i.test(x)).replace(/^cookie:\s*/i,'');
(async()=>{const url='https://ebay-order-manager-lake.vercel.app/api/pocamarket-sync/settings';const before=await(await fetch(url,{headers:{cookie}})).json();const r=await fetch(url,{method:'PUT',headers:{cookie,'content-type':'application/json'},body:JSON.stringify({...before.settings,enabled:true})});const d=await r.json();fs.writeFileSync('.codex-tmp/procurement-settings-resumed.json',JSON.stringify(d));console.log('automatic scheduling resumed',r.status,d.settings?.enabled);})();
