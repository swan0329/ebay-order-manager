const {save}=require('./incident-all-client.cjs');
(async()=>{const call=await require('./shopify-task-client.cjs').client();let cursor=null;const rows=[];do{const j=await call('/graphql.json',{query:`query($cursor:String){productVariants(first:250,after:$cursor){nodes{id sku price inventoryQuantity inventoryPolicy product{id status}} pageInfo{hasNextPage endCursor}}}`,variables:{cursor}});const c=j.data.productVariants;rows.push(...c.nodes);save('shopify',{rows});console.log('shopify variants',rows.length);cursor=c.pageInfo.hasNextPage?c.pageInfo.endCursor:null;}while(cursor)})().catch(e=>{console.error(e.message);process.exitCode=1});
